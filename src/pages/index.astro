---
import MainLayout from '../components/MainLayout.astro';
import Navigation from '../components/Navigation.astro';
import Hero from '../components/Hero';
import Pillars from '../components/Pillars.astro';
import Footer from '../components/Footer.astro';
import '../styles/hero.css';
---

<MainLayout title="DVGE | Dynamic Vector Graphics Engine">
	<Navigation />
	
	<main>
		<Hero client:load />
		<Pillars />
		
		<section class="project-status">
			<div class="status-card">
				<div class="status-grid">
					<div class="status-info">
						<h3>Estado del Proyecto & Licencia</h3>
						<p>
							DVGE es actualmente un proyecto <strong>Open Source</strong> bajo la licencia <strong>MIT</strong>, impulsado de forma independiente. Creemos en la transparencia absoluta durante esta fase de desarrollo (v5.6.0 GA).
						</p>
						<p>
							Como desarrollador único, mi objetivo es transformar este motor en un estándar profesional. Durante esta fase crítica, nos centramos en la madurez técnica y la estabilidad del núcleo para ofrecer la mejor experiencia posible a la comunidad broadcast.
						</p>
					</div>
					<div class="status-roadmap">
						<ul class="roadmap-list">
							<li class="done">✅ v5.6.0: Gestión de Proyectos & Remodelación del layout</li>
							<li>🔲 v5.7.0: Manifest v5.x+ para tools y templates</li>
							<li>🔲 v5.8.0: Panel de Créditos & Mejoras en render queue</li>
							<li>🔲 v5.9.0: Optimizaciones de Shader & Native Previews</li>
							<li>🔲 v6.0.0: Render en Lote</li>
						</ul>
					</div>
				</div>
			</div>
		</section>
	</main>

	<Footer />
</MainLayout>

<style>
	main {
		width: 100%;
	}

	.project-status {
		max-width: 1200px;
		margin: 0 auto;
		padding: 4rem 2rem 8rem;
	}

	.status-card {
		background: rgba(255, 255, 255, 0.03);
		border: 1px solid rgba(255, 255, 255, 0.08);
		border-radius: 24px;
		padding: 3rem;
		backdrop-filter: blur(20px);
	}

	.status-grid {
		display: grid;
		grid-template-columns: 1.5fr 1fr;
		gap: 4rem;
		align-items: center;
	}

	.status-info h3 {
		font-size: 1.8rem;
		margin-bottom: 1.5rem;
		color: #fff;
	}

	.status-info p {
		color: rgba(255, 255, 255, 0.6);
		line-height: 1.7;
		margin-bottom: 1rem;
		font-size: 1.1rem;
	}

	.status-roadmap {
		background: rgba(0, 0, 0, 0.2);
		padding: 2rem;
		border-radius: 16px;
		border: 1px solid rgba(255, 255, 255, 0.05);
	}

	.roadmap-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 12px;
	}

	.roadmap-list li {
		font-size: 0.95rem;
		color: rgba(255, 255, 255, 0.8);
		display: flex;
		align-items: center;
		gap: 10px;
	}

	.roadmap-list li.done {
		color: #4ade80;
		font-weight: 600;
	}

	@media (max-width: 968px) {
		.status-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
		}
		
		.status-card {
			padding: 2rem;
		}
	}
</style>
